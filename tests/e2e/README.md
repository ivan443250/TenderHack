# Cross-runtime smoke tests

This directory will hold tests that exercise Browser → API → Knowledge against Compose. Business scenarios are intentionally deferred until the frozen contracts are wired to real persistence.
