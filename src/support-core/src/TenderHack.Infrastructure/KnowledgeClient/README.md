# Generated Knowledge client boundary

The NSwag-generated client for `docs/contracts/knowledge-v0.openapi.yaml` belongs here. Infrastructure owns transport/serialization; Application consumes its own typed records and must not reference generated FastAPI DTOs directly. Generation is intentionally deferred until the contract export command is wired.
